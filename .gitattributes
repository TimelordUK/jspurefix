# pem encoded test fixtures - keep byte for byte identical on every platform rather than
# letting eol normalisation rewrite them on checkout.
*.crt -text
*.key -text

# scattered component corpus - the whole point of an entry is that its bytes do not
# change, so keep them identical on every platform.
data/corpus/**/*.txt -text
